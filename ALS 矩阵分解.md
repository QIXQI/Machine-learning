晃晃悠悠一个月，昨天终于真正开始学习机器学习了，尽管汇报结果很糟糕，但毕竟开始了😄

ALS 算法是一种协同过滤算法，同时考虑了 User-Item 之间的交互关系，抽象为 <User, Item, Rating> 三元组，Rating 是 User 对 Item 的偏好程度，
假设有 m 个 User 和 n 个 Item, 那么可以构成 R 矩阵，实际应用中， m和n 都很大，而且R矩阵非常稀疏，很容易突破一亿项，因此不易使用传统的方法分解，
因此我们可以假设隐藏因子的存在，将R 矩阵投射到这些关联维度上，用U*P 来逼近 R,
